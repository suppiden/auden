---
import type { Translations } from '../i18n/en';

interface Props {
  t: Translations;
}

const { t } = Astro.props;
const items = t.touchpoints.items;
const row1 = items.slice(0, 5);
const row2 = items.slice(5, 10);
---

<section class="py-20 md:py-28 px-6 md:px-10" style="background: #1A1A1A;" aria-labelledby="touchpoints-headline">
  <div class="max-w-8xl mx-auto">
    <p class="text-[10px] font-medium tracking-[0.25em] uppercase mb-5" style="color: rgba(255,255,255,0.45);" data-reveal>
      {t.touchpoints.eyebrow}
    </p>
    <h2
      id="touchpoints-headline"
      class="text-3xl md:text-4xl lg:text-5xl font-black leading-tight tracking-tight mb-16 md:mb-20"
      style="color: #F0EFE9;"
      data-split-reveal
    >
      {t.touchpoints.headline}
    </h2>

    <!-- Row 1 -->
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 gap-x-8 md:gap-x-12 gap-y-1 mb-6 md:mb-8" data-reveal-stagger>
      {row1.map((item) => (
        <p class="text-sm md:text-base font-normal" style="color: rgba(255,255,255,0.85);">{item}</p>
      ))}
    </div>

    <!-- Row 2 -->
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 gap-x-8 md:gap-x-12 gap-y-1 mb-14 md:mb-16" data-reveal-stagger>
      {row2.map((item) => (
        <p class="text-sm md:text-base font-normal" style="color: rgba(255,255,255,0.85);">{item}</p>
      ))}
    </div>

    <p class="text-[11px] leading-relaxed max-w-2xl" style="color: rgba(255,255,255,0.3);" data-reveal>
      {t.touchpoints.footer}
    </p>
  </div>
</section>
