---
import type { CollectionEntry } from 'astro:content';

interface Props {
  entry: CollectionEntry<'caseStudies'>;
  lang: 'en' | 'es';
}

const { entry, lang } = Astro.props;
const d = entry.data;
const base = lang === 'en' ? '/' : '/es/';
const workUrl = `${base}work/`;

const ytThumb = `https://img.youtube.com/vi/${d.videoId}/maxresdefault.jpg`;
const openLabel = lang === 'en' ? 'Open in SoundCloud →' : 'Abrir en SoundCloud →';

// Deterministic waveform heights for any audio facade.
const waveBars = Array.from({ length: 40 }, (_, i) =>
  22 + Math.round(Math.abs(Math.sin(i * 1.7) * Math.cos(i * 0.6)) * 76)
);

const BG_TOKENS: Record<string, string> = {
  default: 'cs-section--primary',
  alt: 'cs-section--alt',
  craft: 'cs-section--craft',
};

// Map a block's style props to a section class list + inline style.
function sectionClass(block: any, extra = '') {
  const s = block.style ?? {};
  const bg = s.bg ?? 'default';
  const token = BG_TOKENS[bg] ?? 'cs-section--primary';
  return ['cs-section', token, s.border ? 'cs-section--border' : '', extra]
    .filter(Boolean)
    .join(' ');
}
function sectionStyle(block: any) {
  const s = block.style ?? {};
  const out: string[] = [];
  if (s.bg && !BG_TOKENS[s.bg]) out.push(`background: ${s.bg}`);
  if (s.accent) out.push(`--cs-accent: ${s.accent}`);
  if (s.textColor) out.push(`--cs-text: ${s.textColor}`);
  return out.join('; ');
}
function scPlayerSrc(sc: { playlistId: string; secretToken?: string }) {
  const token = sc.secretToken ? `&secret_token=${sc.secretToken}` : '';
  return `https://w.soundcloud.com/player/?url=https%3A%2F%2Fapi.soundcloud.com%2Fplaylists%2F${sc.playlistId}${token}&color=%23ff4d00&auto_play=true&hide_related=true&show_comments=false&show_user=true&show_reposts=false&show_teaser=false&visual=true`;
}
---

<article itemscope itemtype="https://schema.org/Article">

  <!-- ── HERO ──────────────────────────────────────────────────── -->
  <header class="cs-hero">
    <div style="max-width: 64rem; margin: 0 auto;">
      <nav aria-label="Breadcrumb" style="margin-bottom: 3rem;" data-reveal>
        <a href={workUrl} class="cs-back-link" style="font-size: 0.7rem; font-weight: 500; letter-spacing: 0.18em; text-transform: uppercase; text-decoration: none; transition: color 0.2s;">{d.backLabel[lang]}</a>
        <ol style="display: none;" aria-hidden="true">
          <li><a href={base}>Auden</a></li>
          <li><a href={workUrl}>{lang === 'en' ? 'Work' : 'Trabajo'}</a></li>
          <li aria-current="page">{d.title}</li>
        </ol>
      </nav>

      <p class="cs-eyebrow" data-reveal>{d.category} — {d.client}</p>
      <h1 itemprop="headline" class="cs-title" data-split-reveal>{d.title}</h1>
      <p itemprop="description" class="cs-dek" data-reveal>{d.dek[lang]}</p>
    </div>

    <div
      id="yt-facade"
      class="cs-video-wrap"
      role="button"
      tabindex="0"
      aria-label={`Play ${d.title}`}
      data-video-id={d.videoId}
      data-reveal
    >
      <img src={ytThumb} alt={`${d.title} — ${d.dek[lang]}`} loading="lazy" class="cs-video-thumb" />
      <div class="cs-video-overlay" aria-hidden="true"></div>
      <div class="cs-play-wrap" aria-hidden="true">
        <div class="cs-play-btn">
          <svg width="22" height="24" viewBox="0 0 22 24" fill="none" style="margin-left: 3px;">
            <path d="M2 2l18 10L2 22V2z" fill="#fff"/>
          </svg>
        </div>
      </div>
      {d.durationLabel && <span class="cs-duration">{d.durationLabel}</span>}
    </div>

    {d.jumpToScore && (
      <div class="cs-jump" data-reveal>
        <a href={`#${d.jumpToId}`} class="cs-jump-btn" data-scroll-to={d.jumpToId}>
          <span class="cs-jump-icon" aria-hidden="true">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none">
              <path d="M3 14V10M8 18V6M13 15V9M18 20V4M23 13v-2" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"/>
            </svg>
          </span>
          {(d.jumpLabel ?? d.dek)[lang]}
          <span aria-hidden="true">↓</span>
        </a>
      </div>
    )}
  </header>

  <!-- ── BLOCKS ────────────────────────────────────────────────── -->
  {d.blocks.map((block) => {
    if (block.type === 'text') {
      const c = block.content[lang];
      return (
        <section class={sectionClass(block)} style={sectionStyle(block)} aria-label="Case study body">
          <div class="cs-prose" itemprop="articleBody">
            {c.paragraphs.map((group) => (
              <div style="margin-bottom: 2.5rem;" data-reveal>
                {group.map((p) => <p class="cs-body-p">{p}</p>)}
              </div>
            ))}
          </div>
        </section>
      );
    }

    if (block.type === 'quote') {
      const c = block.content[lang];
      return (
        <section class={sectionClass(block)} style={sectionStyle(block)} aria-label="Closing statement">
          <div style="max-width: 52rem; margin: 0 auto; text-align: center;">
            <blockquote style="margin: 0;" data-reveal>
              <p class="cs-closing-text">{c.statement}</p>
              {c.subline && <p class="cs-platform-line">{c.subline}</p>}
            </blockquote>
          </div>
        </section>
      );
    }

    if (block.type === 'numberedList') {
      const c = block.content[lang];
      return (
        <section class={sectionClass(block)} style={sectionStyle(block)} aria-labelledby={`${block.id}-heading`}>
          <div style="max-width: 56rem; margin: 0 auto;">
            <p id={`${block.id}-heading`} class="cs-section-eyebrow" style="margin-bottom: 3.5rem;" data-reveal>{c.label}</p>
            <ol class="cs-craft-list" data-reveal-stagger>
              {c.items.map((point, i) => (
                <li class="cs-craft-item">
                  <span class="cs-craft-num">{String(i + 1).padStart(2, '0')}</span>
                  <div>
                    <strong class="cs-craft-heading">{point.heading}</strong>
                    <p class="cs-craft-body">{point.body}</p>
                  </div>
                </li>
              ))}
            </ol>
          </div>
        </section>
      );
    }

    if (block.type === 'credits') {
      const c = block.content[lang];
      return (
        <section class={sectionClass(block)} style={sectionStyle(block)} aria-label="Credits">
          <div style="max-width: 56rem; margin: 0 auto;" data-reveal>
            <p class="cs-section-eyebrow" style="margin-bottom: 2rem;">{c.label}</p>
            <dl class="cs-credits-dl">
              {block.items.map((credit) => (
                <div>
                  <dt class="cs-credit-role">{credit.role}</dt>
                  <dd class="cs-credit-name">{credit.name}</dd>
                </div>
              ))}
            </dl>
          </div>
        </section>
      );
    }

    if (block.type === 'deliverables') {
      const c = block.content[lang];
      return (
        <section class={sectionClass(block)} style={sectionStyle(block)} aria-label="Deliverables">
          <div style="max-width: 56rem; margin: 0 auto;" data-reveal>
            <p class="cs-section-eyebrow" style="margin-bottom: 1.25rem;">{c.label}</p>
            <p class="cs-deliverables-text">{c.items}</p>
          </div>
        </section>
      );
    }

    if (block.type === 'audio') {
      const c = block.content[lang];
      const scSrc = block.soundcloud ? scPlayerSrc(block.soundcloud) : null;
      return (
        <section id={block.id} class={sectionClass(block)} style={sectionStyle(block)} aria-label="Score">
          <div style="max-width: 56rem; margin: 0 auto;" data-reveal>
            <p class="cs-section-eyebrow" style="margin-bottom: 1.25rem;">{c.title}</p>
            {scSrc ? (
              <>
                <div class="cs-audio">
                  <div id="sc-facade" class="cs-audio-facade" role="button" tabindex="0" aria-label={c.title} data-sc-src={scSrc}>
                    <div class="cs-audio-play" aria-hidden="true">
                      <svg width="18" height="20" viewBox="0 0 22 24" fill="none" style="margin-left: 2px;">
                        <path d="M2 2l18 10L2 22V2z" fill="#fff"/>
                      </svg>
                    </div>
                    <div class="cs-audio-meta">
                      <p class="cs-audio-title">{c.title}</p>
                      <p class="cs-audio-sub">{d.title} · SoundCloud</p>
                    </div>
                    <div class="cs-audio-wave" aria-hidden="true">
                      {waveBars.map((h) => <span style={`height: ${h}%`}></span>)}
                    </div>
                  </div>
                </div>
                {block.link && (
                  <a href={block.link} target="_blank" rel="noopener noreferrer" class="cs-score-link" style="margin-top: 1.5rem;">{openLabel}</a>
                )}
              </>
            ) : block.link ? (
              <a href={block.link} target="_blank" rel="noopener noreferrer" class="cs-score-link">{c.title}</a>
            ) : null}
          </div>
        </section>
      );
    }

    if (block.type === 'about') {
      const c = block.content[lang];
      return (
        <section class={sectionClass(block)} style={sectionStyle(block)} aria-label="About">
          <div style="max-width: 56rem; margin: 0 auto;" data-reveal>
            <p class="cs-about-label">{c.label}</p>
            <p class="cs-about-body">{c.body}</p>
          </div>
        </section>
      );
    }
    return null;
  })}

</article>

<style>
  /* ── Layout sections ── */
  .cs-hero { background: var(--cs-bg); padding: 9rem 1.5rem 4rem; text-align: center; }
  .cs-section { padding: 6rem 1.5rem; }
  .cs-section[id] { scroll-margin-top: 6rem; }
  .cs-section--primary  { background: var(--cs-bg); }
  .cs-section--alt      { background: var(--cs-bg-alt); padding-bottom: 6rem; padding-top: 5rem; }
  .cs-section--craft    { background: var(--cs-bg-craft); }
  .cs-section--border   { border-top: 1px solid var(--cs-border); }

  /* ── Header text ── */
  .cs-eyebrow { font-size: 0.7rem; font-weight: 500; letter-spacing: 0.2em; text-transform: uppercase; color: var(--cs-accent, rgb(255,77,0)); margin-bottom: 1.5rem; }
  .cs-title { font-size: clamp(3rem, 10vw, 8rem); font-weight: 900; line-height: 0.95; letter-spacing: -0.03em; color: var(--cs-text); margin: 0 0 2rem; text-transform: uppercase; }
  .cs-dek { font-size: clamp(1rem, 2.5vw, 1.375rem); font-weight: 400; line-height: 1.6; color: var(--cs-text-dim); max-width: 42rem; margin: 0 auto 4rem; }

  /* ── Video facade ── */
  .cs-video-wrap { max-width: 56rem; margin: 0 auto; position: relative; aspect-ratio: 16/9; background: var(--cs-yt-bg); overflow: hidden; cursor: pointer; }
  .cs-video-thumb { width: 100%; height: 100%; object-fit: cover; display: block; transition: transform 0.6s ease, opacity 0.4s ease; }
  .cs-video-overlay { position: absolute; inset: 0; background: linear-gradient(to top, rgba(0,0,0,0.5) 0%, transparent 60%); pointer-events: none; }
  .cs-play-wrap { position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; pointer-events: none; }
  .cs-play-btn { width: 5rem; height: 5rem; border-radius: 50%; background: rgba(255,77,0,0.9); display: flex; align-items: center; justify-content: center; transition: transform 0.3s ease, background 0.3s ease; box-shadow: 0 0 60px rgba(255,77,0,0.4); }
  .cs-video-wrap:hover .cs-video-thumb { transform: scale(1.03); opacity: 0.85; }
  .cs-video-wrap:hover .cs-play-btn { transform: scale(1.1); background: rgb(255,77,0); }
  .cs-duration { position: absolute; bottom: 1rem; right: 1rem; background: rgba(0,0,0,0.75); color: #fff; font-size: 0.7rem; font-weight: 500; letter-spacing: 0.05em; padding: 0.25rem 0.5rem; backdrop-filter: blur(4px); }

  /* ── Jump-to-score button ── */
  .cs-jump { margin-top: 2.25rem; display: flex; justify-content: center; }
  .cs-jump-btn { display: inline-flex; align-items: center; gap: 0.6rem; font-size: 0.72rem; font-weight: 500; letter-spacing: 0.18em; text-transform: uppercase; color: var(--cs-accent, rgb(255,77,0)); text-decoration: none; border: 1px solid rgba(255,77,0,0.45); border-radius: 999px; padding: 0.7rem 1.5rem; transition: background 0.25s ease, border-color 0.25s ease, transform 0.25s ease; }
  .cs-jump-btn:hover { background: rgba(255,77,0,0.12); border-color: rgb(255,77,0); transform: translateY(-2px); }
  .cs-jump-icon { display: inline-flex; color: var(--cs-accent, rgb(255,77,0)); }

  /* ── Body prose ── */
  .cs-prose { max-width: 42rem; margin: 0 auto; }
  .cs-body-p { font-size: clamp(1rem, 1.5vw, 1.125rem); line-height: 1.85; color: var(--cs-text-body); margin: 0 0 1.25rem; }

  /* ── Closing ── */
  .cs-closing-text { font-size: clamp(1.25rem, 3.5vw, 2.25rem); font-weight: 900; line-height: 1.2; letter-spacing: -0.02em; color: var(--cs-text); margin: 0 0 1.5rem; text-transform: uppercase; }
  .cs-platform-line { font-size: 0.85rem; font-weight: 500; letter-spacing: 0.15em; color: var(--cs-accent, rgb(255,77,0)); text-transform: uppercase; margin: 0; }

  /* ── Craft ── */
  .cs-craft-list { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; }
  .cs-craft-item { padding: 2.5rem 0; border-top: 1px solid var(--cs-border-strong); display: grid; grid-template-columns: 3rem 1fr; gap: 2rem; align-items: start; }
  .cs-craft-num { font-size: 0.75rem; font-weight: 500; color: var(--cs-text-faint); padding-top: 0.35rem; font-variant-numeric: tabular-nums; }
  .cs-craft-heading { display: block; font-size: 1rem; font-weight: 700; color: var(--cs-text); margin-bottom: 0.75rem; letter-spacing: -0.01em; }
  .cs-craft-body { font-size: 0.95rem; line-height: 1.8; color: var(--cs-text-dim); margin: 0; }

  /* ── Section eyebrow ── */
  .cs-section-eyebrow { font-size: 0.7rem; font-weight: 500; letter-spacing: 0.2em; text-transform: uppercase; color: var(--cs-accent, rgb(255,77,0)); margin: 0; }

  /* ── Credits ── */
  .cs-credits-dl { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 1.5rem 2.5rem; }
  .cs-credit-role { font-size: 0.65rem; font-weight: 500; letter-spacing: 0.15em; text-transform: uppercase; color: var(--cs-text-faint); margin-bottom: 0.35rem; }
  .cs-credit-name { font-size: 0.9rem; font-weight: 500; color: var(--cs-text); margin: 0; }

  /* ── Deliverables / about ── */
  .cs-deliverables-text { font-size: 0.9rem; color: var(--cs-text-dim); margin: 0; line-height: 1.6; }
  .cs-score-link { display: inline-flex; align-items: center; gap: 0.5rem; font-size: 0.8rem; font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase; color: var(--cs-accent, rgb(255,77,0)); text-decoration: none; border-bottom: 1px solid rgba(255,77,0,0.4); padding-bottom: 0.15rem; }
  .cs-about-label { font-size: 0.7rem; font-weight: 500; letter-spacing: 0.2em; text-transform: uppercase; color: var(--cs-text-faint); margin: 0 0 0.75rem; }
  .cs-about-body { font-size: 0.85rem; line-height: 1.75; color: var(--cs-text-muted); margin: 0; }

  /* ── Audio facade (SoundCloud) ── */
  .cs-audio { margin-top: 0; }
  .cs-audio-facade { display: flex; align-items: center; gap: 1.25rem; padding: 1.1rem 1.4rem; background: var(--cs-bg-alt); border: 1px solid var(--cs-border-strong); cursor: pointer; transition: background 0.3s ease, border-color 0.3s ease; }
  .cs-audio-facade:hover { background: var(--cs-bg-craft); border-color: rgba(255,77,0,0.45); }
  .cs-audio-play { flex-shrink: 0; width: 3.25rem; height: 3.25rem; border-radius: 50%; background: rgb(255,77,0); display: flex; align-items: center; justify-content: center; transition: transform 0.3s ease, box-shadow 0.3s ease; }
  .cs-audio-facade:hover .cs-audio-play { transform: scale(1.08); box-shadow: 0 0 40px rgba(255,77,0,0.35); }
  .cs-audio-meta { flex-shrink: 0; min-width: 0; }
  .cs-audio-title { font-size: 0.95rem; font-weight: 700; color: var(--cs-text); margin: 0 0 0.2rem; letter-spacing: -0.01em; }
  .cs-audio-sub { font-size: 0.7rem; font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase; color: var(--cs-text-faint); margin: 0; }
  .cs-audio-wave { flex: 1; display: flex; align-items: center; gap: 2px; height: 2.75rem; min-width: 0; overflow: hidden; }
  .cs-audio-wave span { flex: 1; min-width: 2px; background: var(--cs-text-faint); border-radius: 1px; transition: background 0.3s ease; }
  .cs-audio-facade:hover .cs-audio-wave span { background: rgba(255,77,0,0.55); }
  .cs-audio iframe { display: block; border: 0; }
  @media (max-width: 600px) { .cs-audio-wave { display: none; } .cs-audio-meta { flex: 1; } }
</style>

<script>
  // ── YouTube facade → inline player ───────────────────────────
  const facade = document.getElementById('yt-facade') as HTMLElement | null;
  if (facade) {
    function loadVideo() {
      const videoId = facade!.dataset.videoId;
      const iframe = document.createElement('iframe');
      iframe.src = `https://www.youtube-nocookie.com/embed/${videoId}?autoplay=1&rel=0&modestbranding=1`;
      iframe.title = 'Case study video';
      iframe.allow = 'autoplay; encrypted-media; picture-in-picture';
      iframe.allowFullscreen = true;
      iframe.style.cssText = 'position: absolute; inset: 0; width: 100%; height: 100%; border: 0;';
      facade!.style.cursor = 'default';
      facade!.innerHTML = '';
      facade!.appendChild(iframe);
      facade!.removeEventListener('click', loadVideo);
      facade!.removeEventListener('keydown', keyHandler);
    }
    function keyHandler(e: KeyboardEvent) { if (e.key === 'Enter' || e.key === ' ') loadVideo(); }
    facade.addEventListener('click', loadVideo);
    facade.addEventListener('keydown', keyHandler);
  }

  // ── SoundCloud facade → inline player ────────────────────────
  const scFacade = document.getElementById('sc-facade') as HTMLElement | null;
  if (scFacade) {
    function loadAudio() {
      const src = scFacade!.dataset.scSrc;
      if (!src) return;
      const iframe = document.createElement('iframe');
      iframe.src = src;
      iframe.title = 'Score';
      iframe.width = '100%';
      iframe.height = '480';
      iframe.allow = 'autoplay';
      iframe.setAttribute('scrolling', 'no');
      iframe.setAttribute('frameborder', 'no');
      iframe.style.cssText = 'border: 0; display: block; width: 100%;';
      scFacade!.replaceWith(iframe);
    }
    function scKeyHandler(e: KeyboardEvent) { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); loadAudio(); } }
    scFacade.addEventListener('click', loadAudio);
    scFacade.addEventListener('keydown', scKeyHandler);
  }

  // ── Smooth scroll for in-page jump buttons ───────────────────
  document.querySelectorAll<HTMLElement>('[data-scroll-to]').forEach((btn) => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      const id = btn.dataset.scrollTo;
      if (!id) return;
      document.getElementById(id)?.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  });
</script>
